/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{html,js}"],
    darkMode: 'class',
    darkMode: ['class', '[data-mode="dark"]'],
    theme: {
      extend: {},
    },
    plugins: [],
  }